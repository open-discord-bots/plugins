# Enhanced Auto-close Plugin

Adds plugin-level inactivity auto-close for tickets.

Defaults are conservative and avoid overlap with the core autoclose logic when `skipWhenCoreAutocloseEnabled` is true.
