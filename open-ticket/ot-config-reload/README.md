# OT Config Reload
This plugin adds a new command that allows reloading the Open Ticket config files without the need for a restart.

> ### Warning:
> Reloading plugin configurations is supported, but may not work with all plugins.