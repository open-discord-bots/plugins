# Satisfaction Rating Plugin

After a ticket is closed, this plugin posts 1-5 star buttons and stores a single rating per ticket.

Data is stored in `plugins/satisfaction-rating-plugin/ratings.json`.
